ВИКОРИСТАННЯ ШІ ПІД ЧАС РОЗРОБКИ

Під час виконання практичного завдання було використано інструмент штучного інтелекту ChatGPT:
- назва інструменту та версія: ChatGPT (модель GPT-5.3, план Go);
- мета використання: допомога у формуванні структури звіту, уточненні технічних формулювань та систематизації опису патерну проєктування Strategy;
- внесок автора: отримані матеріали були ретельно перевірені, відредаговані та адаптовані відповідно до архітектури, прикладу на C# та вимог.

1. Запит (prompt) для формування розділу 3.2 (Опис обраного патерна):
Опиши поведінковий патерн Strategy.
Потрібно вказати:
- основне призначення патерна;
- випадки застосування;
- переваги та недоліки використання.

Отримана відповідь:
3.2 Опис обраного патерна
Назва патерна: Strategy.
Тип шаблона: поведінковий.
Основне призначення:
Патерн Strategy призначений для визначення сімейства алгоритмів, інкапсуляції кожного з них в окремий клас та забезпечення їх взаємозамінності. Це дозволяє змінювати алгоритм незалежно від клієнта, який його використовує.
Застосування патерна є доцільним у випадках, коли:
- існує декілька способів виконання однієї задачі;
- необхідно уникнути великої кількості умовних операторів (if/else, switch);
- потрібно забезпечити можливість зміни поведінки об’єкта під час виконання програми;
- важливо ізолювати алгоритми від основної логіки програми.
Переваги:
- дотримання принципу Open/Closed;
- ізоляція алгоритмів у окремих класах;
- спрощення тестування;
- використання композиції замість наслідування;
- підвищення гнучкості системи.
Недоліки:
- збільшення кількості класів у програмі;
- необхідність вибору конкретної стратегії клієнтом;
- можливе ускладнення структури для простих задач.

2. Запит (prompt) для формування розділу 3.4 (Реалізація шаблона):
Сформулюй опис реалізації патерну Strategy на мові C# для звіту.
У прикладі реалізовано систему оплати покупок. Потрібно описати:
- створення інтерфейсу IPaymentStrategy;
- конкретні класи стратегій (Card, Cash, PayPal);
- клас контексту ShoppingCart;
- демонстрацію вибору стратегії під час виконання.

Отримана відповідь:
3.4 Реалізація шаблона(патерна)
Реалізація патерна Strategy була виконана мовою програмування C#, було створено інтерфейс для визначення спільної поведінки, а також кілька класів, що реалізують різні варіанти алгоритмів.
Основні етапи:
- створення базового інтерфейсу IPaymentStrategy, який містить метод Pay();
- створено класи CreditCardPayment, CashPayment, PayPalPayment, які реалізують інтерфейс IPaymentStrategy. Кожен із цих класів містить власну реалізацію методу Pay();
- клас ShoppingCart виступає в ролі контексту, який зберігає посилання на інтерфейс стратегії та викликає метод оплати через нього;
- у програмі реалізовано приклад, де користувач може обирати спосіб оплати. В залежності від вибору встановлюється відповідна стратегія, після чого викликається метод Checkout().

Отримані відповіді були проаналізовані та відредаговані відповідно до архітектури системи та прикладу на C#.
У розділі 3.2 було збережено ключові аспекти поведінкового патерну Strategy, уточнено зв’язок теоретичної бази з практичним застосуванням та адаптовано формулювання під академічний стиль. У розділі 3.4 було систематизовано опис етапів реалізації, перевірено відповідність назв класів та методів наданому коду, а також усунено зайві технічні деталі. Формулювання було приведено до єдиного стилю, узгоджено з іншими розділами роботи та перевірено на відповідність структурі патерну, зазначеній у звіті.
